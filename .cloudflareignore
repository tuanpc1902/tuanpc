# Cloudflare Pages ignore file
# Exclude cache files and directories

# Vite cache
.vite/
**/.vite/
cache/
**/cache/

# Node modules (shouldn't be deployed anyway)
node_modules/

# Build artifacts that shouldn't be deployed
*.pack
**/*.pack
dist/